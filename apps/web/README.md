# Composer web

Next.js App Router landing page for Composer.

## Commands

- `npm run dev:web` from the repository root starts this app.
- `npm run build:web` from the repository root builds this app.
- `npm --workspace web run lint` runs ESLint for this app.
